services:
  backend:
    build:
      context: ./src          # 🔹 build context src ফোল্ডার
      dockerfile: Dockerfile  # 🔹 Dockerfile ও src তেই আছে
    container_name: backend
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app            # 🔹 Local src মাউন্ট করে দিচ্ছি
    depends_on:
      - db

  db:
    image: postgres:15
    container_name: fakesongdetection-db-1
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fakesongdb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
